services:
  page-counter:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: page-counter
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
